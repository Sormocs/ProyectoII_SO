#include <stdlib.h>
#include <stdio.h>
#include "text.h"

int main{

	char* = findTrendWord("TextoMaster/bible.txt");
	if (trendWord) {
        printf("Trend word is: %s\n", trendWord);
    } else {
        printf("No trend word found\n");
    }
	
	return 0:

}
